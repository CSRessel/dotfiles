# Personal defaults. Edit this chezmoi source, then apply and run mise install.
[tools]
# Install rustup itself; rustup owns compiler selection (including project pins).
"aqua:rust-lang/rustup" = "latest"
sccache = "latest"

# uv owns Python interpreters and environments.
uv = "latest"
